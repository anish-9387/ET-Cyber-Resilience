# Pinned to a set that was actually installed and imported on Python 3.13 in this
# repo. The previous pins resolved on PyPI but omitted aiohttp and
# langchain-community, both of which the code imports, so the install was broken.
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
python-multipart==0.0.12

sqlalchemy==2.0.36
greenlet==3.1.1
asyncpg==0.30.0
aiosqlite==0.20.0
alembic==1.14.0

neo4j==5.27.0
redis==5.2.1
qdrant-client==1.12.1

numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
scipy==1.14.1
networkx==3.4.2

# Optional: enables real semantic memory. Pulls torch (~2GB). The learning agent
# falls back to a deterministic hashing embedder when this is absent.
sentence-transformers==3.3.1

langchain==0.3.13
langchain-core==0.3.28
langchain-community==0.3.13
langchain-ollama==0.2.2

httpx==0.27.2
aiohttp==3.11.10
websockets==13.1

python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

pyyaml==6.0.2
structlog==24.4.0

pytest==8.3.4
pytest-asyncio==0.24.0
